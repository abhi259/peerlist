export const FIELDTYPE = Object.freeze({
  SHORT: 'shortAnswer',
  LONG: 'longAnswer',
  SINGLE: 'singleSelect',
  URL: 'url',
  DATE: 'date',
});

export const PREVIEWSTATE = Object.freeze({
  true: true,
  false: false,
});
